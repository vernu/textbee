import WebhooksSection from '../(components)/webhooks/webhooks-section'

// Webhook subscriptions management (moved here from the dashboard home).
export default function WebhooksPage() {
  return <WebhooksSection />
}
