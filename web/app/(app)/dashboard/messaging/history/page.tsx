import MessageHistory from '../../(components)/message-history'

// Full-width history view (previously cramped into a half-column tab).
export default function MessageHistoryPage() {
  return <MessageHistory />
}
